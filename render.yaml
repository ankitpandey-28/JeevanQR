# Render.com deployment configuration
services:
  - type: web
    name: qr-emergency-alert
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
